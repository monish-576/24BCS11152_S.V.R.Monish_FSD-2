# Personal Portfolio Dashboard

## Setup
```
npm install
npm run dev
```

## Routes
- / → Home
- /about → About
- /contact → Contact (form redirects to Home on submit)
- /dashboard → Dashboard layout (Profile/Settings links)
- /dashboard/profile → Profile (nested inside Dashboard)
- /dashboard/settings → Settings (nested inside Dashboard)
